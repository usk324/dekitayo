<%= simple_form_for [:manage, child] do |f| %>
  <%= f.input :name %>
  <%= f.input :date_of_birth, html5: true %>
  <%= f.input :photo, as: :file %>

  <div class="d-flex justify-content-center">
    <%= f.submit "Add a Child", class: "btn btn-gradient"%>
  </div>
<% end %>
