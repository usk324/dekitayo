<div class="navbar navbar-expand-sm navbar-light navbar-lewagon">
  <div class="container-fluid">
    <%= link_to root_path, class: "navbar-brand navbar-logo-text text-primary", id: "navbar-logo" do %>
      <div class="logo-image"><%= image_tag "dekitayo-logo.png" %></div>
      DekiTaYo!
    <% end %>
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarSupportedContent">
      <ul class="navbar-nav me-auto">
        <% if user_signed_in? %>
          <%# <li class="nav-item active">
            link_to "Home", "#", class: "nav-link"
          </li>
          <li class="nav-item">
            link_to "Messages", "#", class: "nav-link"
          </li> %>
          <%=link_to rewards_path, class: "rewards-link-btn d-flex nav-link" do %>
            <i class=""></i>
          <% end %>
          <li class="nav-item dropdown">
            <a href="#" class="avatar" id="navbarDropdown" data-bs-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
              <%= cl_image_tag current_user.photo.key, class: "avatar", alt: "dropdown menu" %>
            </a>
            <div class="dropdown-menu dropdown-menu-end" aria-labelledby="navbarDropdown">
              <%= link_to manage_children_path, class:"parent-page-link" do%>
                <i class="fa-solid fa-lock"></i>   Parent Page
              <% end %>
            </div>
          </li>
        <% else %>
          <li class="nav-item d-flex p-0">
            <%= link_to "Login", new_user_session_path, class: "nav-link nav-link-1 text-white" %>
            <%= link_to "Signup", new_user_session_path, class: "nav-link-2 nav-link text-white" %>
          </li>
        <% end %>
      </ul>
    </div>
  </div>
</div>
