
<div id="container">
  <!--Header -->
  <div id="header">
      <h1>  Lovely <%= current_child.name %> Here's your mission 😎</h1>
      <h2><%= @weather['weather'][0]['main']%> in Tokyo<h2>
      <h2> <%=Time.now.strftime("%Y-%m-%d %H:%M") %> <i class="fa-regular fa-clock"></i></h2>
  </div>
  <!--Header-->
  <div class="" style="background-image: url(<%= image_path "snow-background.jpg" %>)">
    <div id ="flexbox">
        <!--flex left -->
      <div class="missions">
        <div class="d-flex justify-content-between">
          <h2><strong>Today's Missions</strong></h2>
          <h1><%=current_child.missions.daily_missions.where(status: "finished").count %>/<%= current_child.missions.daily_missions.size %></h1>
        </div>
        <% if current_child.missions.daily_missions.find_by(status: "started")%>
          <p class="text-center">~You have <%= pluralize(current_child.missions.daily_missions.size, "mission") %> to complete today~</p>
        <% else %>
          <p class="text-center">~No missions available~</p>
        <% end %>
        <% current_child.missions.daily_missions.each do |mission| %>
          <% unless mission.status == "finished"%>
            <%= render partial: "missions/mission_card", locals: { mission: mission } %>
          <% end %>
        <% end %>
      </div>
      <%= link_to "View all Missions", missions_path, class: "btn btn-gradient" %>

        <!--flex right -->
      <div id="right-navigation">
        <h2><strong>Coins💰 <%= current_child.coins %></strong></h2>
        <div id="reward">
                <h2><strong>Rewards</strong></h2>
                <i class="fa-regular fa-teddy-bear"></i>
        </div>

        <div id="gameboard">
        <h1 style="vertical-align=top"> Build your habits and get the special bonus </h1>
        <%= render "shared/gameboard", position: current_child.position %>
        </div>
      </div>
    </div>
  </div>
</div>
